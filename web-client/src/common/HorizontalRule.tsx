import { FC } from 'react';

/**
 * Component for the a horizontal rule in the app's theme.
 */
const HorizontalRule: FC = () => (
    <hr className="border-1 border-neutral-content-default" />
);

export default HorizontalRule;
